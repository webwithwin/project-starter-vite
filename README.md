## Project starter template for a web application built with Vite.

Key Features:

* Frontend Framework: TypeScript + Vite (a modern build tool for fast development)
* Docker Support: Includes both development and production Docker configurations
* Development setup runs on port 3000 with hot module reloading
* Production setup uses Nginx on port 80
* Build Tools: TypeScript compiler with Vite bundling